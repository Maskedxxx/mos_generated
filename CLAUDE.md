# mos_generated — ИИ-генерация документов

Сервис создаёт готовые `.docx` по утверждённым шаблонам заказчика: подставляет реквизиты
организации, должности, ФИО, даты, номера. Дуален сервису проверки — тот ПРОВЕРЯЕТ
те же типы документов, этот их СОЗДАЁТ.

## Куда смотреть

| Что нужно | Где |
|---|---|
| Запуск, конфигурация, API, структура | `README.md` |
| Узлы, контракт API, решения (ADR) | `docs/ARCHITECTURE.md` |
| Как добавить тип (шаблон + генератор) | `docs/ADD_GENERATOR.md` |
| Состояние, история фиксов, грабли шаблонов | `docs/internal/СОСТОЯНИЕ_ГЕНЕРАЦИЯ.md` |
| Техзадание-первоисточник (выполнено) | `docs/internal/ТЗ_доработка.md` |
| Задачи по обоим сервисам | `~/projects/ТРЕКЕР_МосМониторинг.md` (стенд) |
| Соответствие типов каталогу заказчика | `~/Downloads/МАППИНГ_типов_МосМониторинг.md` (у владельца) |
| Сервис проверки (дуальный), общий Docker Compose | `~/projects/mos_analiz_refactor` |
| Скил Claude Code: процедуры и механики обоих сервисов | `mos_analiz_refactor/.claude/skills/mosmonitoring/` (установка: `bash scripts/install_skill.sh` там) |

## Жёсткие правила

1. **После правок кода перезапустить сервис:** `bash scripts/restart.sh` (uvicorn без `--reload`,
   screen `mosgen`, порт из `.env`/`GEN_PORT`, по умолчанию 8090). Шаблоны и `graph.json`
   перечитываются на каждый запрос — для них перезапуск не нужен.
2. **Не коммитить и не пушить без слова владельца.** Идентичность коммитов:
   `Maskedxxx <aangers07@gmail.com>`, без постфикса Claude.
3. **Шаблоны `templates/*.docx` — рабочие файлы заказчика.** Перед правкой делать бэкап
   (`/tmp/tpl_<тип>.bak.docx`), править точечно через zip + regex по `word/document.xml`.
   Метка `{{ключ}}` должна быть одним текстовым фрагментом, иначе docxtpl её не увидит.
4. **Журнал генерации не должен ронять выдачу документа.** Вся запись трейса — под `try/except`.
5. **Стор `_org_store.json` содержит ФИО реальных людей** — в git не попадает; путь задаётся
   `GEN_ORG_STORE`. Не копировать в переписку и не передавать вместе с кодом.
6. **Тесты после правок:** `.venv/bin/pytest -q` (56 тестов, секунды). Новый тип попадает в тесты
   автоматически через реестр.

## Как устроено

**Точка входа одна — `engine.generate(doc_type, session_id, values, client)`**. Через неё проходят
оба HTTP-пути, поэтому логика наследования, стора значений и журналирования пишется только там:

| Эндпоинт | Назначение |
|---|---|
| `POST /api/types/{t}/generate` | генерация по JSON |
| `GET /api/types/{t}/download` | нативное скачивание (браузер сохраняет сам по `Content-Disposition`) |
| `GET /api/graph` | список типов с кодами — фронт рисует выпадающий список |
| `GET /api/types/{t}/schema` | схема полей: `{key, label, required, source, hint, default}` |
| `GET /api/orgs`, `GET /api/org/suggest?org=` | автозаполнение по организации |

**15 типов**, все — Word через `docxtpl`. Генератор типа лежит в `generators/<тип>.py`
и содержит `SCHEMA` (поля формы) и `context()` (подготовка значений под шаблон).
Реестр — `registry.py`, граф с кодами — `graph.json`.

**Автозаполнение по организации.** Стор (`GEN_ORG_STORE`, по умолчанию `_org_store.json`):
ключ — нормализованное название (`engine.normalize_org` снимает юрформу, кавычки, пунктуацию,
регистр), значение — `поле → список ранее введённых значений`. Пополняется в конце каждой генерации.
Перенос между типами работает по совпадающим ключам полей.

**Журнал генерации.** На каждый вызов пишется `logs_generated/<тип>/session_<время>/`
(`GEN_TRACES_DIR`): `meta.json` (время, тип, IP, статус, размер), `values.json`, копия `.docx`.
Отказы тоже журналируются со `status: "error"`. Счёт выпущенного:
`find logs_generated -maxdepth 2 -type d -name "session_*" | wc -l`.

## Стек

Python 3.12, FastAPI + uvicorn, `docxtpl` + `python-docx`. Языковая модель НЕ используется.
Фронт: React-экран в составе объединённого интерфейса проверки (основной) и простой
`static/index.html` для ручной проверки (отдаётся самим FastAPI по `/`).

## Дерево проекта

```
api.py               HTTP-слой: эндпоинты
engine.py            движок: сессии, стор по организациям, журнал трейсов
registry.py          реестр генераторов
graph.json           типы с кодами (0.1 … 3.10) — порядок в интерфейсе
generators/<тип>.py  SCHEMA + context() под конкретный документ
templates/<тип>.docx шаблон с плейсхолдерами
scripts/             restart.sh, healthcheck.sh
tests/               pytest: генераторы, стор ООО, API
docs/                ARCHITECTURE.md, ADD_GENERATOR.md
Dockerfile           образ для общего docker-compose в репозитории проверки
logs_generated/      трейсы генераций (в git не попадают)
_org_store.json      накопленные значения по организациям (не в git)
_etalons/ _out/      эталоны и образцы для сверки при разработке (не в git)
static/index.html    простой фронт
```

## Запуск и доступ

`bash scripts/restart.sh` → `:8090`, screen `mosgen`. Домены: `https://mosaudit.ru.tuna.am` →
раздел «Генерация» (основной, через прокси `/gen`) и `https://mosgen.ru.tuna.am` (простой фронт, fallback).
Docker: `docker compose up -d --build` из `mos_analiz_refactor` (этот репозиторий — сервис `gen`).

Стенд `ms-llm-dev` — команда `dev` (SSH `nemovm@172.16.10.64`, корпоративный OpenVPN).
Репозиторий: `git@github.com:Maskedxxx/mos_generated.git`, разработка в ветке `dev`.
